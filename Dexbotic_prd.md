# Dexbotic 产品介绍页 · 网站产品需求文档

| 字段 | 内容 |
|---|---|
| 文档版本 | v2.0（专业完整版）|
| 日期 | 2026-04-24 |
| 页面路由 | `/solutions/dexbotic` |
| 作者 | 产品团队 |
| 状态 | 待评审 |
| 目标上线 | 待定 |

---

## 目录

1. [产品背景与目标](#一产品背景与目标)
2. [用户研究](#二用户研究)
3. [竞品分析](#三竞品分析)
4. [信息架构与页面结构](#四信息架构与页面结构)
5. [各模块详细设计规格](#五各模块详细设计规格)
6. [内容策略与文案规范](#六内容策略与文案规范)
7. [交互设计规范](#七交互设计规范)
8. [视觉设计规范](#八视觉设计规范)
9. [技术要求](#九技术要求)
10. [成功指标与埋点](#十成功指标与埋点)
11. [开发优先级与分期](#十一开发优先级与分期)

---

## 一、产品背景与目标

### 1.1 背景

Dexbotic 是基于 PyTorch 的开源 VLA（Vision-Language-Action）模型工具箱，由 Dexmal 原力灵机开发并维护。已完成核心能力建设：支持 π0、OpenVLA-OFT、CogACT、MemoryVLA 等前沿策略，覆盖单臂、双臂及导航多种机器人形态，并提供 DM0 预训练基座（LIBERO 成功率 95%，RoboChallenge 全球第一）。

**当前传播现状痛点**：

- 传播方式：GitHub 仓库 + PDF 教程，信息高度分散
- 品牌认知：开发者不知道 Dexbotic 和竞品的差异点
- 上手门槛：环境配置复杂，PDF 教程阅读体验差，无法搜索
- 社区缺失：无统一的 Q&A、案例展示、贡献者激励入口
- 转化断层：感兴趣 → 跑通第一个训练之间缺少引导

### 1.2 产品目标

| 层次 | 目标 | 衡量标准 |
|---|---|---|
| 认知层 | 让新访客 5 分钟内理解 Dexbotic 是什么、能做什么、比竞品好在哪 | 首屏跳出率 < 45% |
| 转化层 | 让有目标的开发者 30 分钟内跑通 LIBERO 训练 | GitHub clone 转化率 > 8% |
| 留存层 | 沉淀社区内容，让开发者在遇到问题时回来查文档 | 人均页面访问 > 2.5 |
| 生态层 | 成为 VLA 领域最知名的开源工具箱入口 | 月 UV 达到 1w+（上线 3 个月）|

### 1.3 非目标（本期不做）

- 在线训练或推理执行（所有计算在用户本地/云端）
- 用户账号与付费体系
- 自建 Q&A 社区（通过 GitHub Discussions 外链承接）
- 多语言版本（中文优先，英文占位）

---

## 二、用户研究

### 2.1 核心用户画像

#### Persona A：高校算法研究生「小周」（P0，最核心）

- **背景**：计算机视觉/机器人学方向，熟悉 PyTorch、LLM、VLM，有 Python 基础
- **场景**：需要复现 π0 或 DM0 论文，跑 LIBERO/CALVIN benchmark
- **核心诉求**：
  - 快速跑通 baseline，不想花超过 2 小时在环境配置上
  - 能自定义替换视觉编码器或动作头，验证自己的想法
  - 有据可查的评测结果（LIBERO 成功率、RC 真机榜单）
- **痛点**：
  - 各个 VLA 框架环境不互通，每次都要从头配
  - 论文代码复现难度高，各框架数据格式不一
  - 不知道选哪个预训练基座能跑出最好结果
- **决策路径**：`Google 搜索 "VLA toolkit" → 看 GitHub Stars → 找快速上手示例 → 看 Benchmark → 决定用`
- **关键页面**：Hero → 核心能力 → 快速开始 → Benchmark → GitHub

#### Persona B：企业机器人应用工程师「李工」（P1）

- **背景**：从事机械臂或移动底盘产品开发，Python 熟练，VLA 经验有限
- **场景**：公司引入 VLA 方案，需要用现有机械臂数据微调并部署
- **核心诉求**：
  - 能直接套用预训练模型到自家机械臂（UR5 或 Franka）
  - 数据格式转换简单，不要求重新标注
  - 有商业友好的开源协议（MIT）
- **痛点**：
  - 不确定自己的机械臂数据能否接入
  - 担心开源协议限制商用
  - 真机效果不确定，想先看 demo
- **决策路径**：`同事/社区推荐 → 看支持哪些机械臂型号 → 看商用协议 → 看真机案例 → 决定试用`
- **关键页面**：支持硬件型号 → Dexdata 格式 → RC 真机评测 → 开源协议

#### Persona C：学生/初学者「小明」（P2）

- **背景**：CV 或 LLM 背景，第一次接触 VLA，好奇具身智能
- **场景**：想了解 VLA 是什么，找到可以跑通的最小示例
- **核心诉求**：
  - 有清晰的概念介绍，不需要看论文
  - 有可以复制粘贴的代码，跑通一次就有成就感
  - 看到实际效果（视频/动图）
- **痛点**：
  - 概念太多太抽象，不知道从哪里开始
  - 显卡不够，担心跑不起来
  - 英文文档看不懂
- **决策路径**：`B 站/知乎推荐 → 首页 → VLA 基础介绍 → 快速开始（LIBERO）→ 放弃或继续`

#### Persona D：企业技术决策者/投资人「张总」（P3）

- **背景**：评估具身智能领域技术方案，关注成熟度与生态
- **核心诉求**：技术背书、社区规模、商业落地可行性
- **关键页面**：Hero Stats → Benchmark → 生态合作伙伴 → 关于 Dexmal

### 2.2 用户旅程地图（核心路径 A 为例）

```
[发现] → [评估] → [上手] → [深度使用] → [贡献/传播]

发现：
  渠道：Google/Bing 搜索 "open source VLA toolkit"、arXiv/论文引用、GitHub Trending
  触点：首页 Hero、Benchmark 截图、GitHub README

评估（5分钟决策窗口）：
  问题1：这东西是什么？→ 一句话定位 + 3 个核心能力
  问题2：比 OpenVLA/LeRobot 好在哪？→ 差异化对比区
  问题3：真的好用吗？→ LIBERO 95%、RC 全球第一的数据
  问题4：多久能跑通？→ 30分钟快速开始
  关键：如果 5 分钟内找不到答案，会直接离开

上手（30分钟目标）：
  Step1 环境配置（5分钟）→ 复制 Docker 命令
  Step2 下载模型+数据（10分钟）→ HF 镜像命令
  Step3 启动训练（15分钟）→ torchrun 命令
  障碍：OOM 报错 → 内联 FAQ 解决，不跳出页面

深度使用：
  替换自定义数据集 → Dexdata 格式文档
  调整超参 → Trainer Config 说明
  提交 RC 评测 → RC 集成教程

贡献/传播：
  PR 提交新机型适配 → GitHub 链接
  分享成果 → 引导发推/发论文引用 Dexbotic
```

---

## 三、竞品分析

### 3.1 直接竞品对比

| 维度 | Dexbotic | LeRobot (HuggingFace) | OpenVLA | RoboFlamingo |
|---|---|---|---|---|
| **定位** | VLA 训练+评测全链路工具箱 | 机器人数据+训练统一平台 | 开源 VLA 微调框架 | 语言条件操控 |
| **预训练基座** | DM0（2.4B，SOTA）| 无（依赖 HF 模型）| OpenVLA-7B | Flamingo |
| **数据格式** | Dexdata（Video+JSONL）| LeRobot Dataset | 无统一格式 | 无 |
| **支持策略** | π0 / OFT / CogACT / MemoryVLA / DM0 | ACT / π0 | OFT 为主 | 单一 |
| **真机评测** | RC 平台（全球真机）| 无统一评测 | 无 | 无 |
| **硬件门槛** | RTX 4090 起（1卡推理）| 无明确要求 | 7B 参数，门槛高 | 高 |
| **文档质量** | 需要提升（当前 PDF）| 较好 | 一般 | 差 |
| **Star 数（参考）** | 待填 | ~9k | ~5k | ~1k |
| **商用协议** | MIT | Apache 2.0 | Apache 2.0 | 待查 |

### 3.2 Dexbotic 差异化核心

基于以上分析，Dexbotic 有且仅有以下三个真正的差异化优势：

1. **DM0 预训练基座 = 显著更好的起点**：同等数据量下，使用 DM0 基座训练出来的模型性能显著优于使用旧基座的竞品。这是最核心的竞争力。

2. **Dexdata = 最低的数据接入成本**：统一的 Video + JSONL 格式，内置 10+ 数据集转换脚本，自定义机械臂数据接入只需写一个配置文件。

3. **RC 真机评测 = 独有的闭环验证能力**：Dexbotic 与 RoboChallenge 深度集成，是目前唯一能从"本地仿真训练"直通"全球真机榜单评测"的开源工具箱。

> **设计原则**：页面的每一个模块都必须服务于上述三个差异化点之一。不是差异化的内容可以弱化展示。

---

## 四、信息架构与页面结构

### 4.1 页面整体框架

本页面（`/solutions/dexbotic`）是 Dexbotic 产品的核心着陆页，定位为**产品介绍页（Product Landing Page）**，不是文档页，目标是让访客理解价值并产生行动（clone/star/试用）。

**页面高度预估**：约 5500–6500px（桌面端），阅读时间约 4–6 分钟。

### 4.2 模块顺序与逻辑

```
屏位  模块名称               核心目的
─────────────────────────────────────────────────────
1     Hero Banner           建立认知：这是什么，解决什么问题
2     核心数字 Stats         建立信任：用数字证明价值
3     问题切入区             共鸣：你是否有这些痛点？
4     解决方案 = 五大能力    说服：Dexbotic 如何解决
5     技术架构图             深度：给技术用户的信心
6     前沿策略支持           广度：支持的模型范围
7     Dexdata 格式           可操作性：自定义数据怎么接
8     硬件与机型支持         可行性：我的设备能用吗
9     快速开始（代码块）      行动：30 分钟跑通
10    RC 真机评测             验证：仿真之后怎么测真机
11    性能 Benchmark         背书：真实数据证明效果
12    生态与社区              信任：有活跃社区，不是孤岛
13    CTA 底部               最终行动：选你要做什么
```

---

## 五、各模块详细设计规格

---

### Module 01 · Hero Banner

#### 目的
第一屏 5 秒内完成两件事：①让访客理解"这是什么工具"；②给出清晰的下一步行动。

#### 布局规格
- 全宽背景（100vw），高度 `min-h-[90vh]`，不超过 `max-h-[800px]`
- 内容区 `max-w-5xl mx-auto`，水平居中
- 背景：深色（slate-950 / `#020817`），配代码流动粒子动画（轻量级，不影响 LCP）
- 左侧宽度 `~55%` 文字区，右侧 `~45%` 视觉区

#### 内容清单（从上到下）

```
[徽章行]
  ├── 图标：GitHub mark
  ├── 文字："开源 · MIT License"
  └── 右侧：版本号标签 "v2.0"

[主标题]
  DexboticH1，font-extrabold，text-7xl，白色
  
[英文副标题]
  "The Open-Source VLA Toolkit"
  text-3xl，text-blue-300，font-light

[核心价值句]
  "从数据处理、模型训练到真机评测，一站式解决 VLA 开发全链路问题"
  text-xl，text-slate-300，max-w-xl

[行动按钮组]
  主按钮：「快速开始」（蓝色实底，带 →）
  次按钮：「GitHub 仓库」（深色边框）
  文字链接：「查看 Benchmark →」（蓝色文字，无边框）

[信任信号行]
  「已服务 1000+ 开发者 · 数十家高校/企业 · MIT License」
  text-xs，text-slate-500，间距分隔
```

#### 右侧视觉区
- 内容：终端风格的代码动画，循环展示三行关键命令（git clone / docker run / torchrun），带闪烁光标
- 或：机械臂执行任务的 GIF/视频（自动播放，静音，循环）
- 边框：深色玻璃态卡片（border border-white/10，backdrop-blur）

#### 交互行为
- 主按钮 hover：背景色变浅（blue-500），轻微上移（translateY -1px）
- 代码区每 3s 切换到下一条命令，带淡入淡出动画
- 滚动超过 80px 后，Hero 底部 Stats 区渐入（intersection observer）

#### 移动端适配
- 右侧视觉区在 `< md` 时折叠到文字下方
- 按钮改为全宽（`w-full`）
- 标题字号降至 `text-4xl`

#### 文案（精确版）

```
徽章：开源 VLA 工具箱 · MIT License · v2.0

主标题：Dexbotic

英文副标题：The Open-Source VLA Toolkit
            for Real-World Robotics

核心价值句：
基于 PyTorch 构建，从数据格式统一、模型训练到真机评测，
一站式解决 VLA 开发全链路问题——让你专注研究，而不是基础设施。

主按钮：快速开始  →
次按钮：GitHub 仓库
文字链接：查看 Benchmark →

信任行：已服务 1000+ 开发者  ·  数十家高校/企业  ·  全球首个开源真机评测闭环
```

---

### Module 02 · 核心数字 Stats Bar

#### 目的
用 4 个精心选择的数字，在 5 秒内建立技术可信度。

#### 布局规格
- 紧跟 Hero，`border-t border-white/10`，深色背景延续
- 4 列等宽，`divide-x divide-white/10`
- 每列：`py-8 px-6 text-center`

#### 数字内容

| 数字 | 单位/颜色 | 标签 |
|---|---|---|
| `5` | 蓝色 | 支持前沿 VLA 策略 |
| `10+` | 蓝色 | 支持机器人本体型号 |
| `95%` | 绿色 | LIBERO 任务成功率 |
| `#1` | 金色/amber | RoboChallenge 全球排名 |

#### 数字展示规范
- 数字字号：`text-5xl font-black`，颜色差异化（不全用蓝色）
- 标签字号：`text-xs uppercase tracking-[0.12em]`，slate-400

#### 交互行为
- 进入视口时，数字从 0 count-up 到目标值，动画时长 800ms，easing: ease-out
- hover 单列：轻微背景高亮（white/5）

---

### Module 03 · 问题切入区（Why Dexbotic）

#### 目的
用共鸣式写法，描述开发者在 VLA 研发中真实遇到的痛点，然后给出 Dexbotic 的解决方案。避免直接自夸，而是从用户问题出发。

#### 布局规格
- 白色背景（首个白色区块），`py-20`
- 左侧：痛点列表（占 `col-span-5`）
- 右侧：Dexbotic 解法（占 `col-span-7`），强调色彩区分

#### 左侧痛点列表（3 条，带❌icon）

```
❌ 环境配置太复杂
   每个 VLA 框架都有自己的依赖，各种版本冲突，
   光配环境就要花一天——还不一定成功。

❌ 数据格式千差万别
   UR5 的数据、Franka 的数据、ALOHA 的数据……
   格式互不兼容，转换脚本要自己写。

❌ 预训练基座已过时
   很多框架还在用 2023 年的老基座，
   基座落后意味着训练出来的模型性能有明显天花板。
```

#### 右侧解法区（对应 3 条，带✅icon）

```
✅ Docker 一键部署，5 分钟启动
   一条 docker run 命令，环境自动配置完毕，
   conda activate dexbotic，开始工作。

✅ Dexdata 统一数据格式
   Video + JSONL 标准格式，内置 10+ 数据集转换脚本，
   自定义数据只需修改一个注册文件。

✅ DM0 基座：LIBERO 成功率 95%
   使用最新 DM0 预训练权重作为起点，
   在 RoboChallenge 真机评测中全球排名第一。
```

#### 视觉设计
- 两列之间有细分隔线（`border-x`），或用背景色差异区分
- 痛点列用 `text-slate-400` 弱化，解法用 `text-slate-900` 强化
- 每条高度一致，对齐展示"问题→解法"的映射关系

---

### Module 04 · 五大核心能力

#### 目的
系统展示 Dexbotic 的技术能力全景，让专业用户建立完整认知。

#### 布局规格
- 浅灰背景（`bg-slate-50`），`py-20`
- 标题居中，`mb-12`
- 能力卡片：`2×3` 网格（桌面），最后一行如为奇数则居中
- 卡片：`bg-white rounded-2xl p-7 border border-slate-200`，hover 轻抬起

#### 模块标题

```
上标：Core Capabilities（蓝色等宽字体）
主标题：五大核心能力
副标题：覆盖 VLA 开发从数据到评测的完整链路
```

#### 能力卡片详细规格（6 张）

---

**卡片 1：统一模块化架构**
- 图标：Layers（蓝色）
- 标题：统一模块化架构
- 一句话：VLM + Action Expert 解耦，自由替换任意组件
- 详细说明：
  ```
  将复杂的 VLA 策略拆解为标准组件：
  · 视觉编码器（CLIP-ViT / DINOv2，可替换）
  · LLM 骨干（Qwen2.5-VL，可替换）
  · 动作专家（扩散策略 / 自回归，可替换）
  
  三层完全解耦，改一层不影响其他层，
  让你专注验证自己的创新点，而不是重写框架。
  ```
- 标签：`架构 · 模块化`

---

**卡片 2：强大预训练基座（DM0）**
- 图标：Zap（金色/amber）
- 标题：强大预训练基座
- 一句话：DM0，LIBERO 95%，RoboChallenge 全球第一
- 详细说明：
  ```
  许多开源框架仍使用 2023 年的旧基座。
  Dexbotic 内置 DM0 预训练权重（2.4B 参数），
  
  在主流评测中显著优于同类：
  · LIBERO：成功率 95%
  · RoboChallenge Table30：62.0%（全球第一）
  
  更好的起点 = 更少的数据 = 更快达到目标成功率。
  ```
- 标签：`SOTA · 2.4B`

---

**卡片 3：Dexdata 统一数据格式**
- 图标：Database（靛蓝）
- 标题：Dexdata 统一数据格式
- 一句话：Video + JSONL，10+ 数据集一键转换
- 详细说明：
  ```
  自定义数据接入只需三步：
  1. 确保数据含视频帧、state、prompt、is_robot 字段
  2. 使用内置脚本转换为 Dexdata 格式
  3. 注册一个配置文件，开始训练
  
  支持 LIBERO · CALVIN · RoboTwin · ManiSkill2
  · LeRobot SO-101 · 自定义机械臂数据
  ```
- 标签：`数据 · 标准化`

---

**卡片 4：多机器人多平台支持**
- 图标：Bot（紫色）
- 标题：10+ 机型，全硬件覆盖
- 一句话：消费级 4090 到云端 H100，任意选
- 详细说明：
  ```
  机器人：UR · Franka · ARX · UMI · ALOHA
          Galaxea R1 · Realman · DOS-W1 · WidowX · LeRobot SO-101
  
  硬件：RTX 4090（1卡推理）/ A100 · H100（8卡训练）
  云端：阿里云 · 火山引擎 GPU 集群
  系统：Ubuntu 20.04 / 22.04 + NVIDIA Docker
  ```
- 标签：`硬件 · 多平台`

---

**卡片 5：简单的配置体系**
- 图标：Settings（绿色）
- 标题：一个 Exp 脚本，全部搞定
- 一句话：不再深陷复杂配置，改一个文件就换一个实验
- 详细说明：
  ```
  传统 VLA 框架配置复杂：环境变量、超参文件、
  模型路径……改一个参数要动多个文件。
  
  Dexbotic：复制 libero_dm0 模板脚本，
  修改以下几行即可启动新实验：
  · dataset_name：换数据集
  · model_name_or_path：换基座
  · num_train_steps：改训练步数
  · output_dir：设置保存路径
  ```
- 标签：`效率 · 开发体验`

---

**卡片 6：完整的真机评测闭环**
- 图标：Trophy（金色）
- 标题：直通 RoboChallenge 真机评测
- 一句话：从仿真训练到真机榜单，一条命令提交
- 详细说明：
  ```
  仿真成功率 ≠ 真机成功率。
  Dexbotic 与 RoboChallenge 原生集成：
  
  内置 RC 评测接口
  → 克隆评测组件（1 条命令）
  → 配置任务 yaml（2 行改动）
  → 后台提交评测（nohup 挂起）
  → 真机榜单实时查看
  
  目前唯一支持此完整闭环的开源 VLA 工具箱。
  ```
- 标签：`评测 · RC · 真机`

#### 卡片交互
- hover：`translateY(-2px)`，`shadow-md`，`border-blue-200`
- 点击：展开详细说明（默认折叠，节省空间），或跳转对应文档锚点
- 每张卡片底部有"了解详情 →"文字链接

---

### Module 05 · 技术架构图

#### 目的
给技术用户提供一张清晰的"内部结构图"，建立对 Dexbotic 设计哲学的深度信任。

#### 布局规格
- 白色背景，`py-20`
- 上方：标题 + 副标题（居中）
- 主体：SVG 架构图（`max-w-4xl mx-auto`，约 `800×480px`）
- 下方：3 列文字说明（对应架构三层）

#### 架构图内容（SVG 设计规格）

```
┌──────────────────────────────────────────────────────────────┐
│                       INPUT LAYER（输入层）                    │
│  [👁 视觉 · 3路视频]  [🗣 语言指令]  [🤖 机器人状态]           │
└──────────┬────────────────┬──────────────────┬──────────────┘
           │                │                  │
           ▼ (CLIP-ViT 编码) ▼                  ▼
┌──────────────────────────────────────────────────────────────┐
│                   VISION ENCODER（视觉编码器）                 │
│         CLIP-ViT-Large-Patch14-336（可替换为 DINOv2 等）      │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                  LLM BACKBONE（语言骨干）                      │
│              Qwen2.5-VL 1.7B（DM0 默认配置）                  │
│         ← 可替换为其他兼容 LLM，无需改动上下游代码 →          │
└────────────┬────────────────────────────┬────────────────────┘
             │                            │
    ┌────────▼──────────┐    ┌────────────▼──────────────────┐
    │  自回归动作头      │    │     扩散策略动作专家            │
    │  Autoregressive   │    │  Action Expert（~300M）        │
    │  适合长程分步任务  │    │  输出平滑连续轨迹               │
    │                  │    │  适合精细灵巧操作               │
    └────────┬──────────┘    └────────────┬──────────────────┘
             └────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                      ACTION OUTPUT（动作输出）                 │
│    关节角度序列  /  末端执行器位姿  /  离散高级指令              │
└──────────────────────────────────────────────────────────────┘
```

#### 图下方 3 列文字说明

| 列 | 标题 | 内容 |
|---|---|---|
| 列 1 | 自由替换每一层 | 视觉编码器、LLM 骨干、动作头均可独立替换，接口标准化，无需修改底层代码 |
| 列 2 | 双策略并存 | 同时支持自回归（长程任务）和扩散策略（精细操作），根据任务类型自动或手动选择 |
| 列 3 | 多源数据统一训练 | 模仿学习（IL）+ 强化学习（RL）数据在同一框架下联合训练，提升模型泛化性 |

---

### Module 06 · 前沿策略支持

#### 目的
展示 Dexbotic 支持的策略宽度，帮助用户选择，并强化"覆盖面最广的工具箱"的定位。

#### 布局规格
- 浅灰背景，`py-16`
- 标题 + 表格（主视图）+ 决策树（辅助）

#### 标题
```
上标：Supported Strategies
主标题：支持 5 种前沿 VLA 策略
副标题：选择适合你任务的策略，其余配置 Dexbotic 帮你搞定
```

#### 策略对比表格

| 策略 | 类型 | 最适合场景 | 数据量要求 | 推理速度 | 推荐指数 |
|---|---|---|---|---|---|
| **DM0**（推荐）| 扩散策略 + LLM | 通用操作 / Generalist | 中（500条+）| 快 | ⭐⭐⭐⭐⭐ |
| **π0（Pi0）** | 扩散策略 | 精细灵巧操作 | 多（2000条+）| 中 | ⭐⭐⭐⭐ |
| **OpenVLA-OFT** | 自回归 + LoRA | 数据量少，快速验证 | 少（<500条）| 慢 | ⭐⭐⭐ |
| **CogACT** | 扩散策略 | 视觉感知精度高的任务 | 中 | 中 | ⭐⭐⭐ |
| **MemoryVLA** | 带记忆机制 | 长程多步骤任务 | 多 | 较慢 | ⭐⭐⭐ |

#### 策略选择决策树（交互式，Tab 切换）

```
你的情况是？
│
├── [我刚开始，不知道选什么] → 选 DM0，有最好的预训练权重
│
├── [我的任务需要毫米级精度] → 选 π0 或 DM0
│
├── [我的数据量很少（<500条）] → 选 OpenVLA-OFT，用 LoRA 微调
│
├── [我的任务有超过 10 个步骤] → 选 MemoryVLA，有记忆机制
│
└── [我想参加 RC 评测，看真实榜单] → 选 DM0，有最高成功率
```

---

### Module 07 · Dexdata 数据格式详解

#### 目的
解决"我能不能用自己的数据"这个高频疑虑，让有自定义数据的用户感到"这个可以搞定"。

#### 布局规格
- 白色背景，`py-20`
- 上方：标题 + 一句话说明
- 中间：左侧步骤说明（`col-span-5`）+ 右侧代码块（`col-span-7`）
- 下方：支持数据集卡片行（横向滚动）

#### 内容

**标题区**：
```
上标：Data Format
主标题：Dexdata——具身数据的统一语言
副标题：无论你的数据来自什么机械臂、什么仿真器，都能用 3 步接入 Dexbotic
```

**左侧 3 步流程**：

```
Step 01  准备原始数据
─────────────────────
必须包含的核心字段：
· images_1/2/3：视频流（记录 frame_idx）
· state：机械臂当前位姿向量
· prompt："pick up the red cube"（文本指令）
· is_robot: true（标识这是机器人数据）

可选字段（有更好）：
· action：目标位姿（无监督时可省略）
· subtask：子任务描述
· goal_bbox：目标物体边界框

Step 02  转换为 Dexdata 格式
─────────────────────────────
使用内置脚本一键转换：
· LeRobot 数据集：script/convert_data/lerobot.py
· RLDS 数据集：script/convert_data/rlds.py
· 自定义格式：参照模板编写转换脚本

Step 03  注册到 Dexbotic
─────────────────────────
在 dexbotic/data/data_source/ 下
添加一个注册文件，定义数据路径和采样频率，
即可在训练脚本中通过名称引用。
```

**右侧代码块（JSONL 格式示例）**：
```json
{
  "images_1": {
    "type": "video",
    "url": "path/to/episode_001/cam_wrist.mp4",
    "frame_idx": 21
  },
  "images_2": {
    "type": "video",
    "url": "path/to/episode_001/cam_front.mp4",
    "frame_idx": 21
  },
  "images_3": {
    "type": "video",
    "url": "path/to/episode_001/cam_side.mp4",
    "frame_idx": 21
  },
  "state": [0.12, -0.34, 1.57, 0.08, -0.22, 0.91],
  "prompt": "pick up the red cube and place it in the box",
  "is_robot": true,
  "action": [0.15, -0.31, 1.60, 0.10, -0.19, 0.88],
  "subtask": "approach target",
  "goal_bbox": [0.32, 0.41, 0.58, 0.67]
}
```

**下方支持的数据集 chips（横向标签行）**：
```
LIBERO  ·  CALVIN  ·  RoboTwin 2.0  ·  ManiSkill2
·  LeRobot SO-101  ·  RLDS  ·  自定义格式（模板可用）
```

---

### Module 08 · 硬件与机器人型号支持

#### 目的
帮助用户判断"我的设备/机器人能不能用"，降低使用门槛。

#### 布局规格
- 浅灰背景，`py-16`
- 左侧：硬件配置要求表（`col-span-5`）
- 右侧：机器人型号图标阵列（`col-span-7`）

#### 硬件配置表

```
系统要求
──────────
OS：Ubuntu 20.04 / 22.04
容器：NVIDIA Docker（强烈推荐）
驱动：CUDA 11.8+

配置方案
──────────────────────────────────────────────
用途          | 最低配置        | 推荐配置
─────────────────────────────────────────────
单卡推理/评测 | RTX 4090 × 1   | RTX 4090 × 1
本地训练      | RTX 4090 × 4   | RTX 4090 × 8
大规模训练    | A100 80G × 8   | H100 × 8
─────────────────────────────────────────────
云端平台：阿里云 · 火山引擎 · 自建集群
```

#### 机器人型号阵列（图标 + 名称）

```
单臂操作
─────────────────────────────
[UR5/UR10] [Franka Panda] [ARX]
[WidowX] [UMI] [Realman]

双臂操作
─────────────────────────────
[ALOHA] [Galaxea R1] [Dexmal DOS-W1]

移动底盘
─────────────────────────────
[LeRobot SO-101] [轮式双臂（导航+操作）]

开源硬件（自研）
─────────────────────────────
[DOS-W1：Dexmal 开源双臂机器人，BOM+图纸+SDK 全开源]
```

**DOS-W1 特别标注**（带徽章 "自研开源"）：
```
Dexmal DOS-W1 是原力灵机自研并开源的双臂机器人实验平台，
Dexbotic 对其提供原生最优支持。
开源内容：硬件 BOM · 三维图纸 · CMF 规范 · 全套 SDK · 数据采集→训练→推理全流程
```

---

### Module 09 · 快速开始（核心转化模块）

#### 目的
这是页面中转化率最关键的模块。目标：让访客复制代码，30 分钟内跑通一次训练，产生"它确实能用"的信心。

#### 布局规格
- 深色背景（`bg-slate-900`），`py-20`
- 上方：标题 + 时间承诺
- 主体：3 个步骤，每步带：步骤编号 + 说明文字 + 可复制代码块
- 右侧：结构卡片（展示什么文件/步骤）
- 下方：3 个跳转链接（完整文档）

#### 标题区
```
上标：Quick Start
主标题：30 分钟跑通你的第一次 VLA 训练
副标题：以 LIBERO 数据集 + DM0 基座为例，适合首次上手的最小可行路径
```

#### 3 步代码块详细内容

**Step 1：配置环境**
```
说明：使用 Docker 部署，无需手动处理依赖冲突。
预计耗时：~5 分钟
```
```bash
# 1. 克隆代码库
git clone https://github.com/dexmal/dexbotic.git

# 2. 启动 Docker 容器（推荐）
docker run -it --rm --gpus all \
  --network host \
  --userns=host \
  -v $(pwd)/dexbotic:/dexbotic \
  dexmal/dexbotic bash

# 3. 激活环境
cd /dexbotic
conda activate dexbotic
pip install -e .
```

**Step 2：下载模型与数据**
```
说明：使用 HF 镜像加速下载，国内用户无需科学上网。
预计耗时：~10 分钟（取决于网速）
```
```bash
# 配置镜像源（国内加速）
export HF_ENDPOINT=https://hf-mirror.com

# 下载 DM0 预训练基座
mkdir -p checkpoints/libero && cd checkpoints/libero
git clone https://hf-mirror.com/Dexmal/DM0-libero DM0-libero

# 下载 LIBERO 数据集
cd ../../
mkdir -p data && pip install huggingface_hub
hf download Dexmal/libero --repo-type=dataset --local-dir dexbotic/data
cat libero.tar.part-* > libero.tar && tar -xvf libero.tar

# 下载 CLIP 视觉编码器
hf download openai/clip-vit-large-patch14-336
```

**Step 3：启动训练**
```
说明：修改两行路径配置，8 卡分布式训练启动。
预计耗时：训练约 80000 步（时间取决于显卡配置）
```
```bash
# 配置 CLIP 路径
export OPENAI_CLIP_PATH="path/to/clip-vit-large-patch14-336"

# 启动 8 卡训练
torchrun --nproc_per_node=8 \
  path/to/your_training.py \
  --task train
```

#### 代码块 UI 规格
- 深色背景（`#0d1117`，GitHub 代码风格）
- 右上角：一键复制按钮（点击后显示"已复制 ✓"，2s 后复位）
- 行号显示（`01 02 03...`）
- 语法高亮：命令关键字蓝色，参数绿色，注释灰色
- 步骤间用编号徽章 `01 / 02 / 03` 区分

#### 内联 FAQ（OOM 问题）

在 Step 3 下方折叠展示：

```
▼ 遇到 Out of Memory（OOM）错误？

两步解决：
1. 减小 per_device_train_batch_size（默认 4，改为 2 或 1）
2. 启用 ZeRO-3 Offload：将 deepspeed 配置从 zero3.json 
   改为 zero3_offload.json

如仍有问题，查看完整 FAQ 或提交 GitHub Issue →
```

#### 底部链接区
```
[📖 完整训练教程]  [🔬 推理与评测教程]  [🔧 自定义数据接入指南]
```

---

### Module 10 · RC 真机评测闭环

#### 目的
展示 Dexbotic 独有的仿真→真机闭环能力，这是最强的差异化点之一。

#### 布局规格
- 白色背景，`py-20`
- 上方：大标题 + 说明（居左对齐，`col-span-6`）+ 右侧配图（RC 平台截图，`col-span-6`）
- 下方：6 步流程（横向步骤条）

#### 标题区
```
上标：Real-Robot Evaluation
主标题：仿真训练好了，怎么验证真机效果？
说明文字：
  仿真成功率 ≠ 真机成功率。
  Dexbotic 原生集成 RoboChallenge 评测接口——
  全球首个大规模真机评测平台，使用真实场景的机械臂测试，
  让你的 LIBERO 95% 在真实世界得到验证。
  
  目前唯一支持此完整闭环的开源 VLA 工具箱。
```

#### 6 步真机评测流程

```
① 注册账号
   robochallenge.cn 注册，
   获取 API Token

② 克隆评测组件
   git clone .../Dexbotic-
   RoboChallengeInference.git

③ 安装依赖
   在 Docker 环境内
   pip install -r requirements.txt

④ 配置任务
   修改 configs/specialist/
   put_cup_on_coaster.yaml
   填写 checkpoint 路径

⑤ 创建评测任务
   点击 RC 平台
   "Eval Your Policy"
   记录 Run ID

⑥ 提交评测
   nohup python execute.py \
   --config-name=specialist/... \
   user_id=TOKEN \
   job_collection_id=RUN_ID
```

#### 配置文件示例（代码块）
```yaml
# put_cup_on_coaster.yaml
defaults:
  - ../default
task_name: put_cup_on_coaster
checkpoint: /path/to/your/model   # ← 只需改这一行
action_horizon: 25
```

#### 右侧配图建议
- RC 平台榜单截图（模糊处理真实用户数据）
- 或：Table 30 任务列表截图
- 或：真机执行任务的视频截图

---

### Module 11 · 性能 Benchmark

#### 目的
用真实数据背书效果，打消"开源模型效果不行"的顾虑。

#### 布局规格
- 浅蓝底色（`bg-[#f0f5ff]`），`py-20`
- 左侧：标题 + 说明（`col-span-5`）
- 右侧：柱状图可视化（`col-span-7`，高度约 380px）

#### 标题
```
上标：Benchmark
主标题：真机评测，2.4B 参数超越 7B 模型
副标题：数据来源：RoboChallenge Table 30 公开榜单（Generalist 多任务模式）
```

#### 柱状图数据

| 模型 | 参数量 | 成功率 | 亮色 |
|---|---|---|---|
| DM0（使用 Dexbotic） | 2.4B | **62.0%** | 蓝色（高亮，带"全球第一"角标）|
| Pi0.5 | 3.3B | 42.67% | 浅蓝 |
| Pi0 | 3.3B | 28.33% | 灰蓝 |
| OFT | 7.0B | 8.66% | 灰色 |

#### 关键洞察文字（配图下方）

```
🔑 参数更少，效果更好
   DM0（2.4B）成功率 62.0%，
   是 OFT（7.0B）的 7.2 倍，
   且部署门槛更低（RTX 4090 即可）

🔑 为什么 Dexbotic 的 DM0 更好？
   · 从零开始的具身多模态训练（不是迁移学习）
   · 空间推理思维链（5 阶段链路输出）
   · 联合训练多源数据（仿真+真实+互联网多模态）
```

---

### Module 12 · 生态与社区

#### 目的
建立"这是一个活跃的、可持续的开源项目"的印象，降低长期依赖的顾虑。

#### 布局规格
- 白色背景，`py-16`
- 三区块横排：社区数据 / 更新日志 / 合作伙伴

#### 社区数据（左，`col-span-3`）

```
1000+    数字大号，蓝色
活跃开发者

数十家
合作高校/企业

32 篇
相关论文发表

MIT License
免费商业使用
```

#### 更新日志（中，`col-span-5`）

```
时间轴形式，显示最近 5 条：

2026-04  v2.0 发布
         DM0-base v2.0，支持 Qwen2.5-VL 编码器

2026-04  社区贡献
         @contributor 提交 UR5 适配器，已合并主分支

2026-02  RC 全球第一
         DM0 在 RoboChallenge Table30 双项第一

2025-11  Dexbotic 2.0
         支持 5 大具身原生特性，服务数十家机构

2025-10  论文发布
         Dexbotic 论文发表，引用量持续增长

[查看完整更新日志 →]
```

#### 合作伙伴（右，`col-span-4`）

```
Logo 展示（灰度，hover 恢复彩色）：
· Hugging Face（模型托管）
· 清华大学（联合研发）
· 无问芯穹/Rlinf（RL 训练）
· RoboChallenge 组委会
  （含智源研究院、智元机器人、Qwen 等）
· 阿里巴巴（战略投资方）
```

---

### Module 13 · CTA 底部召唤

#### 目的
页面末尾的最终行动召唤，针对不同意图的用户分流。

#### 布局规格
- 浅蓝渐变背景（`from-blue-50 to-indigo-50`），`py-24`
- 居中布局，三个不同深度的行动入口

#### 内容
```
主标题：选择你的开发路径

[路径 1] 我要训练模型         [路径 2] 我要接入自己的数据
  ↓                              ↓
[快速开始]              [Dexdata 格式文档]

[路径 3] 我要参加 RC 真机评测
  ↓
[RC 评测教程]

──────────────────────────────────────────

[⭐ GitHub Star]  [📖 完整文档]  [💬 加入社区]

──────────────────────────────────────────

联系方式
商务合作：contact@dexmal.com
GitHub Issues 技术支持
MIT License，核心代码免费商用
```

---

## 六、内容策略与文案规范

### 6.1 品牌语调

Dexbotic 面向的是有技术背景的工程师和研究者，语调应该：

| 维度 | 要 | 不要 |
|---|---|---|
| 专业度 | 技术准确，不泛化 | 技术术语滥用，显得堆砌 |
| 语气 | 平等、直接，工程师对工程师 | 居高临下，或过于销售感 |
| 证明方式 | 数据、代码、可复现的结果 | 形容词堆砌（"革命性"、"划时代"）|
| 中英混用 | 技术词汇保留英文（LIBERO、torchrun、JSONL）| 整段英文 |

### 6.2 标题写作原则

- **功能性标题**：直接说能做什么（"3 步跑通 LIBERO 训练"比"快速上手"好）
- **数字锚定**：能用数字就用数字（"5 分钟配置环境"比"快速配置"好）
- **对比锚定**：借助竞品或行业痛点建立参照（"比 OpenVLA 更简单"比"简单易用"好）

### 6.3 核心文案库（可直接用于设计稿）

```
产品定义：
  EN: "The Open-Source VLA Toolkit for Real-World Robotics"
  CN: "开源 VLA 工具箱，从数据到真机一站搞定"

差异化一句话：
  "DM0 基座 + Dexdata 格式 + RC 真机评测，
   目前最完整的具身 AI 开发闭环"

能力概括：
  "5 种策略 · 10+ 机型 · 消费级显卡可用 · MIT 开源"

快速开始承诺：
  "30 分钟跑通你的第一次 VLA 训练"

数据背书：
  "LIBERO 成功率 95% · RoboChallenge 全球第一"

社区信任：
  "已服务 1000+ 开发者，数十家高校/企业使用中"
```

---

## 七、交互设计规范

### 7.1 滚动行为

| 元素 | 触发条件 | 动画 |
|---|---|---|
| Stats 数字 | 进入视口时 | count-up，800ms，ease-out |
| 能力卡片 | 进入视口时 | 从下方淡入（translateY 20px → 0），stagger 100ms |
| 代码块 | 进入视口时 | 淡入，无位移 |
| Benchmark 柱状图 | 进入视口时 | 柱子从 0 高度生长到目标高度，800ms |

### 7.2 代码块交互

- 悬停：显示复制按钮（右上角）
- 点击复制：按钮变为"已复制 ✓"，绿色，2 秒后恢复
- 多步代码块：Tab 切换（Step 1 / Step 2 / Step 3）

### 7.3 能力卡片展开

- 默认：显示标题 + 一句话说明
- hover：显示"了解详情 ↓"按钮
- 点击：在卡片内展开详细内容（max-height 动画），不跳转页面
- 再次点击：收起

### 7.4 策略对比表格

- 默认：完整展示所有行
- 推荐策略行（DM0）：蓝色背景高亮，带"推荐"角标
- hover 行：轻微蓝色高亮背景

### 7.5 锚点导航（长页固定导航）

当用户滚动超过 Hero 区时，页面顶部出现一条细导航条（`position: sticky`），包含：
```
Dexbotic  |  核心能力  架构  数据格式  快速开始  评测  Benchmark  社区  |  [GitHub ⭐]
```
点击任意锚点，平滑滚动到对应模块。当前所在模块对应项高亮。

---

## 八、视觉设计规范

### 8.1 色彩系统

| Token | 色值 | 用途 |
|---|---|---|
| `brand-blue` | `#2563eb` | 主按钮、链接、强调 |
| `brand-indigo` | `#4f46e5` | 次级强调、架构图 |
| `brand-cyan` | `#06b6d4` | 代码高亮关键字 |
| `success-green` | `#10b981` | 成功率数字、✅icon |
| `warning-amber` | `#f59e0b` | 推荐标签、Trophy icon |
| `bg-dark` | `#020817` | Hero、代码块背景 |
| `bg-page` | `#f8fafc` | 交替浅灰区块 |
| `text-primary` | `#0f172a` | 主标题 |
| `text-secondary` | `#475569` | 正文 |
| `text-muted` | `#94a3b8` | 辅助说明、标签 |

### 8.2 字体排版

| 级别 | 规格 | 用途 |
|---|---|---|
| Hero H1 | `text-7xl / 72px, font-extrabold` | 产品名 |
| Hero 副标题 | `text-3xl, font-light` | 英文定位句 |
| Section H2 | `text-4xl, font-extrabold` | 模块主标题 |
| 卡片 H3 | `text-xl, font-bold` | 能力卡片标题 |
| 正文 | `text-base / 16px, leading-relaxed` | 说明文字 |
| 代码 | `font-mono, text-sm / 14px` | 代码块 |
| 标签/说明 | `text-xs, uppercase, tracking-wider` | 上标标签 |

### 8.3 圆角与阴影

| 元素 | 规格 |
|---|---|
| 能力卡片 | `rounded-2xl, shadow-sm`，hover: `shadow-md` |
| 代码块 | `rounded-xl` |
| 按钮 | `rounded-lg` |
| 标签 badge | `rounded-full` |
| 模块大容器 | `rounded-3xl`（特殊区块如 Benchmark）|

### 8.4 间距体系

- 模块间距：`py-20`（默认）/ `py-16`（次要模块）
- 内容最大宽度：`max-w-7xl`（1280px）
- 网格：`gap-8`（默认）/ `gap-5`（卡片网格）

---

## 九、技术要求

### 9.1 性能指标

| 指标 | 目标值 |
|---|---|
| LCP（最大内容绘制）| < 2.5s |
| FID（首次输入延迟）| < 100ms |
| CLS（累积布局偏移）| < 0.1 |
| 首屏 JS Bundle | < 200KB（gzip）|
| 图片格式 | WebP，带 AVIF 降级 |

### 9.2 SEO 要求

| 元素 | 规格 |
|---|---|
| `<title>` | `Dexbotic · 开源 VLA 模型工具箱 · Dexmal` |
| `<meta description>` | `基于 PyTorch 的开源 VLA 工具箱，支持 DM0·π0·OpenVLA 等5种策略，LIBERO成功率95%，RoboChallenge全球第一，30分钟跑通训练。` |
| H1 | 全页唯一，包含"Dexbotic" |
| 图片 alt | 所有图片必须有描述性 alt |
| 代码块 | 使用 `<pre><code>` 语义标签 |
| 结构化数据 | SoftwareApplication schema |

### 9.3 无障碍要求

- 所有颜色对比度满足 WCAG AA（4.5:1）
- 所有交互元素键盘可访问
- 屏幕阅读器可以正确读取代码块内容
- 动画组件提供 `prefers-reduced-motion` 降级

---

## 十、成功指标与埋点

### 10.1 核心 KPI

| 指标 | 当前基线 | 目标（上线 3 个月）|
|---|---|---|
| 月 UV | 待测量 | 10,000+ |
| 首屏跳出率 | 待测量 | < 45% |
| GitHub 跳转点击率 | 待测量 | > 8% |
| 代码复制次数 | 0（新功能）| > 500/月 |
| 「快速开始」模块完整阅读率 | 待测量 | > 35% |
| 平均页面停留时长 | 待测量 | > 3 分钟 |

### 10.2 关键埋点事件

```
页面级：
· page_view（含 utm_source / utm_medium）
· page_scroll_depth（25% / 50% / 75% / 100%）

交互级：
· hero_cta_click（区分主/次按钮）
· github_link_click（区分来源模块）
· code_copy_click（区分 Step 1/2/3）
· capability_card_expand（记录哪张卡片被展开）
· strategy_selector_click（记录选了哪个策略）
· benchmark_section_view（进入 Benchmark 区）
· rc_cta_click
· bottom_cta_click（区分三条路径）
```

---

## 十一、开发优先级与分期

### Phase 1（必须上线，MVP）

| 优先级 | 模块 | 原因 |
|---|---|---|
| P0 | Hero Banner | 第一屏，决定跳出率 |
| P0 | 快速开始代码块 | 核心转化模块 |
| P0 | 五大核心能力 | 核心说服内容 |
| P0 | Benchmark 图表 | 最强背书 |
| P1 | 问题切入区 | 增强共鸣 |
| P1 | RC 真机评测 | 差异化核心 |
| P1 | 技术架构图 | 给技术用户信心 |

### Phase 2（迭代增强）

| 模块 | 内容 |
|---|---|
| 策略对比决策树 | 交互式，需要额外开发 |
| Dexdata 格式详解 | 可先用静态展示 |
| 社区动态 | 需要对接 GitHub API |
| 锚点导航条 | 体验增强 |

### Phase 3（长期运营）

| 模块 | 内容 |
|---|---|
| 真实用户案例 | 需要收集用户素材 |
| 视频 Demo | 需要制作内容 |
| RC 实时榜单 | 需要对接 RC API |
| 多语言版本 | 英文版 |

---

## 附录：关键文案速查表

```
产品定位：
  "基于 PyTorch 的开源 VLA 模型工具箱"
  "The Open-Source VLA Toolkit for Real-World Robotics"

三大差异化（记住这三个）：
  1. DM0 基座：LIBERO 95%，RC 全球第一
  2. Dexdata：Video + JSONL，10+ 数据集一键转换
  3. RC 闭环：唯一支持仿真→真机完整链路的开源工具箱

五大能力（对应卡片）：
  统一模块化架构 · 强大预训练基座 · Dexdata 数据格式
  · 10+ 机型多平台 · 一个 Exp 脚本全搞定 · RC 真机评测

数字背书：
  95%（LIBERO）· 62.0%（RC，全球第一）· 2.4B（参数）
  5（策略）· 10+（机型）· 1000+（开发者）· 32（相关论文）

硬件门槛：
  最低：RTX 4090 × 1（推理/评测）
  推荐训练：8 × A100/H100

快速开始承诺：
  "30 分钟跑通你的第一次 VLA 训练"

开源协议：
  MIT License，核心代码免费商用
```

---

*文档由产品团队基于《Dexbotic 教程.pdf》《Dexbotic 介绍及宣传 2.pdf》及 company.txt 整理。数据以公开渠道实际数据为准，设计稿以此文档为基准，如有变更请同步更新本文档。*
